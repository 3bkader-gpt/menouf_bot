# 🚀 دليل نشر البوت على Render.com

## 📋 الخطوات العملية (مخصصة لبوتك)

---

## **المرحلة الأولى: تحضير الـ Repository**

### 1. في GitHub:
- ✅ تأكد إن الـ repo بتاعك **public** (أو سجّل في Render بـ GitHub account)
- ✅ تأكد إن فيه `requirements.txt` في الـ root folder
- ✅ يكون في الـ repo: `bot.py` الرئيسي
- ✅ تأكد إن `.env` موجود في `.gitignore` (مش محتاج نرفعه)

### 2. في `requirements.txt`:
✅ الملف جاهز ويحتوي على:
- `python-telegram-bot==22.5`
- `firebase-admin==6.5.0`
- `python-dotenv==1.0.0`
- `cachetools==5.3.3`
- `flask==3.0.0` (للـ health check)

---

## **المرحلة الثانية: إعداد Render**

### 3. في Render.com:
- اعمل account جديد (أو سجّل بـ GitHub)
- اضغط **"New"** → اختار **"Web Service"**

### 4. اختار الـ Repository:
- اختار الـ GitHub repo بتاعك
- اضغط **"Connect"**

### 5. في الإعدادات (مهم جداً):

| الحقل | القيمة |
|-------|--------|
| **Name** | `uploader-bot` (أو أي اسم تفضله) |
| **Runtime** | `Python 3` |
| **Region** | `Frankfurt` (أو الأقرب ليك) |
| **Branch** | `main` (أو `master`) |
| **Root Directory** | اتركه فارغ (أو `/` إذا كان في subfolder) |
| **Build Command** | `pip install -r requirements.txt` |
| **Start Command** | `python bot.py` |
| **Plan** | `Free` (للبداية) |

### 6. اضغط "Create Web Service":
- Render بتشتغل الـ deploy تلقائياً
- انتظر **3-5 دقائق** لحد ما يكمل

---

## **المرحلة الثالثة: البيئة والـ Secrets**

### 7. في Render Dashboard:
- اذهب لـ service بتاعك
- ادخل على **"Environment"** (في القائمة الجانبية)
- أضيف المتغيرات التالية:

| المتغير | القيمة | ملاحظات |
|---------|--------|---------|
| `TELEGRAM_TOKEN` | `8463136615:AAHR3wZB5a72F_kS8VLb1lMz7S-NA_9momA` | توكن البوت |
| `TELEGRAM_ADMIN_CHANNEL_ID` | `-1003070315274` | معرف القناة |
| `FIREBASE_SERVICE_ACCOUNT_JSON` | (انسخ الـ JSON كامل) | انظر الملاحظة أدناه |
| `ADMIN_PASSWORD` | `MyS3curePass!23` | كلمة سر لوحة التحكم |
| `PORT` | `5000` | (اختياري - Render بيحدده تلقائياً) |

**ملاحظة مهمة عن `FIREBASE_SERVICE_ACCOUNT_JSON`:**
- انسخ محتوى `firebase-credentials.json` بالكامل
- الصقه في المتغير `FIREBASE_SERVICE_ACCOUNT_JSON` **كـ JSON string واحد**
- مثال:
  ```
  {"type":"service_account","project_id":"menofbot",...}
  ```

### 8. اضغط "Save Changes":
- Render بتعيد تشغيل الـ service تلقائياً
- انتظر **1-2 دقيقة** لحد ما يشتغل

---

## **المرحلة الرابعة: Health Check (جاهز تلقائياً ✅)**

### 9. البوت جاهز:
✅ تم إضافة **Flask health check server** في `bot.py`:
- Endpoint: `https://your-service.onrender.com/`
- Health endpoint: `https://your-service.onrender.com/health`
- الـ server بيشتغل في background thread
- **لا حاجة لتعديلات إضافية**

---

## **المرحلة الخامسة: UptimeRobot (الحل للـ Sleep)**

### 10. في UptimeRobot.com:
- اعمل account مجاني: https://uptimerobot.com/
- اضغط **"Add New Monitor"**

### 11. في الإعدادات:

| الحقل | القيمة |
|-------|--------|
| **Monitor Type** | `HTTP(s)` |
| **Friendly Name** | `Uploader Bot` |
| **URL** | `https://your-service-name.onrender.com/health` |
| **Monitoring Interval** | `Every 5 minutes` |
| **Alert Contacts** | (اختياري) أضف إيميلك |

### 12. اضغط "Create Monitor":
- UptimeRobot بيبدأ يبعت requests كل 5 دقائق
- هذا بيخلي Render **مش ينام** البوت

---

## **المرحلة السادسة: التحقق**

### 13. في Render Logs:
- ادخل على الـ service
- اضغط **"Logs"** (في الأعلى)
- شوف الرسائل التالية:
  ```
  Health check server started on port 5000
  Bot is starting...
  ```
- تأكد **لا في errors** (خاصة Firebase initialization)

### 14. في Telegram:
- اختبر البوت: اضغط `/start`
- تأكد إن يرد بسرعة
- جرب navigation (اختر برنامج → ترم → مادة)
- تأكد إن البحث شغال

### 15. في UptimeRobot:
- تأكد إن الـ Monitor status = **Up** ✅
- بيبعت notifications لو في مشاكل

---

## ✅ **التلخيص:**

| الخطوة | الخدمة | ايه اللي تعمله |
|--------|--------|----------------|
| 1-2 | GitHub | تحضير الـ repo + `requirements.txt` ✅ |
| 3-8 | Render | إنشاء service + إضافة environment variables |
| 9 | البوت | Health check جاهز تلقائياً ✅ |
| 10-12 | UptimeRobot | إضافة monitor لـ Render |
| 13-15 | تحقق | اختبار + تأكد من الشغل |

---

## ⏱️ **الوقت المتوقع:**
- **20-30 دقيقة** إذا كل حاجة جاهزة ✅

---

## 🔧 **استكشاف الأخطاء:**

### المشكلة: "Failed to initialize Firebase"
**الحل:** 
- تأكد إن `FIREBASE_SERVICE_ACCOUNT_JSON` صحيح (JSON كامل بدون أخطاء)
- جرب تحويله إلى string واحد (بدون أسطر جديدة)

### المشكلة: "Bot is not responding"
**الحل:**
- شوف Render Logs عشان تشوف الأخطاء
- تأكد إن `TELEGRAM_TOKEN` صحيح
- تأكد إن البوت مش banned من Telegram

### المشكلة: "Service sleeping"
**الحل:**
- تأكد إن UptimeRobot شغال ويبعت requests
- جرب زيارة الـ URL يدوياً: `https://your-service.onrender.com/health`

### المشكلة: "Port already in use"
**الحل:**
- Render بيحدد الـ PORT تلقائياً
- تأكد إن `PORT` متغير موجود في Environment (أو اتركه Render يحدده)

---

## 📝 **ملاحظات مهمة:**

1. **Free Tier Limitations:**
   - Render Free tier **ينام بعد 15 دقيقة** عدم استخدام
   - UptimeRobot بيحل المشكلة دي (يبعت requests كل 5 دقائق)

2. **Environment Variables:**
   - **لا ترفع** `.env` على GitHub
   - استخدم Render Environment Variables فقط

3. **Health Check:**
   - الـ health check server بيشتغل تلقائياً
   - مش محتاج تعديلات إضافية

4. **Logs:**
   - Render Logs مفيدة جداً للـ debugging
   - شوفها لو في أي مشاكل

---

## 🎯 **الخطوات التالية:**

بعد ما البوت يشتغل:
1. ✅ اختبر كل الـ commands
2. ✅ تأكد إن Firebase شغال
3. ✅ راقب الـ logs لمدة يوم
4. ✅ (اختياري) ارفع للـ Paid plan لو محتاج uptime أفضل

---

**آخر تحديث:** 2025-01-19

